# Guacamole Manager Environment Configuration
# 
# NOTICE: This file is now DEPRECATED!
# Configuration is managed through 00_Guacamole-Manager_Config.json
# 
# The deployment scripts automatically generate .env files from the JSON configuration.
# Please edit 00_Guacamole-Manager_Config.json instead of this file.
#
# This file is kept for reference and backward compatibility only.

# Database Configuration (now in JSON: database.password)
POSTGRES_PASSWORD=your_secure_postgres_password_here

# LDAP/Active Directory Configuration (now in JSON: ldap.*)
LDAP_HOSTNAME=your-ad-server.domain.com
LDAP_PORT=389
LDAP_USER_BASE_DN=CN=Users,DC=yourdomain,DC=com
LDAP_USERNAME_ATTRIBUTE=sAMAccountName
LDAP_GROUP_BASE_DN=CN=Users,DC=yourdomain,DC=com

# Network Configuration (now in JSON: deployment.network.*)
GUACAMOLE_SUBNET=172.22.18.0/24
GUACAMOLE_GATEWAY=172.22.18.1

# Synology Configuration (now in JSON: deployment.synology.*)
SYNOLOGY_VOLUME_PATH=/volume1/guacamole

# SSL Configuration (now in JSON: ssl.*)
SSL_CERT_PATH=./config/nginx/ssl/cert.pem
SSL_KEY_PATH=./config/nginx/ssl/key.pem

# Default Admin Credentials (now in JSON: guacamole.*)
DEFAULT_ADMIN_USERNAME=guacadmin
DEFAULT_ADMIN_PASSWORD=guacadmin